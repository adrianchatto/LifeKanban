**Done.** Ticket numbers like `k-052` now show on cards in `index.html`:

- Board cards show a small ticket ID badge above the title.
- List rows show the ticket ID beside the status.
- Calendar items include the ticket ID in the visible chip text and tooltip.
- The edit modal shows the ticket ID beside “Edit card”.

I also updated `docs/USER_GUIDE.md` and its changelog.

Verification completed:
- HTML parsed successfully.
- JavaScript extracted from `index.html` passed `node --check`.
- Python files passed `py_compile` with cache redirected to `/tmp`.

I couldn’t start the local server or use the in-app browser from this sandbox, so visual browser verification wasn’t possible here.