# k-050 — Add projects from Add by Chat

**Done.** Add by Chat now lets you create a new project directly in the project picker flow, instead of only choosing an existing project.

When the chat asks **“Which project?”**, the picker includes **＋ New...**. Enter a project name, press **Add** or **Enter**, and it:

- adds the project to the board
- assigns its colour
- persists it
- selects it for the pending task
- lets you add the card under that new project

Result saved to `results/k-050.md`, and the board card is marked done. Verification passed: `board.json ok` and `checked 5 inline scripts`.