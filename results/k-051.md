# k-051 — Pomodoro box across the bottom

**Done.** The Pomodoro timer is now a full-width bottom bar in `index.html`.

Changed:
- Pomodoro is pinned across the whole bottom of the board.
- Timer controls are arranged horizontally.
- Settings open upward from the bar.
- Board content gets bottom padding so it is not hidden.
- Add-by-chat is raised above the Pomodoro bar.
- Mobile layout wraps cleanly.

Verification:
- Pomodoro inline script passes `node --check`.
- Python app files pass `py_compile` with cache redirected to `/tmp`.

Result saved to `results/k-051.md`.